# grid_sorter
 Accepts multiple grid-level csv files as an input, output will be all the grid-level data sorted geographically based on the given .shp file. Thresholds can also be identified per data column. By default, it uses Multi-processing to speed up the computation time. 
